# ComponentLibraryImplementation
An implementation for a component library based on PrimeVue with a custom made theme, wrapping basic and advanced components to provide options to add additional props and variables on top of PrimeVue
